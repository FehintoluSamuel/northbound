@* FocusLayout.razor — minimal layout for immersive/focused pages
   (currently just Lessons.razor). No top nav, no bottom tab bar — the
   page itself draws its own full-height chrome, same reasoning as
   why Auth pages get their own separate layout. *@
@inherits LayoutComponentBase

@Body
